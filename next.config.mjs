/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
       urlImports: ['https://fonts.googleapis.com','https://images.unsplash.com']
    },
};

export default nextConfig;
